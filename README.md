# Hyperresearch

Agent-driven research knowledge base. Browse, collect, and synthesize web sources into a searchable wiki with `[[wiki-links]]`, full-text search, and structured JSON output.

```bash
pip install hyperresearch
hyperresearch init .
hyperresearch search "query" -j
```
